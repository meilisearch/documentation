---
title: "Create an API key"
description: "POST /keys — Meilisearch API keys API reference."
openapi: post /keys
---

Create an API key.

<Note>
This endpoint requires the master key for authorization.
</Note>

<Tip>
You can create keys with specific `actions` to limit what operations they can perform, and `indexes` to restrict which indexes they can access.
</Tip>

[Learn more about API keys.](/products/platform/basic_security)