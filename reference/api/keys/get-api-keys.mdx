---
title: "Get all API keys"
description: "GET /keys — Meilisearch API keys API reference."
openapi: get /keys
---

Get all API keys. Results can be paginated using `offset` and `limit` query parameters.

<Note>
This endpoint requires the master key for authorization.
</Note>

[Learn more about API keys.](/products/platform/basic_security)