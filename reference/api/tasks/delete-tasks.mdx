---
title: "Delete tasks"
description: "DELETE /tasks — Meilisearch tasks API reference."
openapi: delete /tasks
---

Delete a set of tasks based on their uid, status, type, indexUid, or the date at which they were enqueued, started, or finished.

<Warning>
Task deletion is an irreversible operation. Only `succeeded`, `failed`, or `canceled` tasks can be deleted.
</Warning>

[Learn more about asynchronous operations.](/products/platform/tasks)