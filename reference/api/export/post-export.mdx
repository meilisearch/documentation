---
openapi: post /export
---

Export documents from one or more indexes. The export is streamed as NDJSON (Newline Delimited JSON).

<Note>
This endpoint is useful for backing up documents or migrating data between Meilisearch instances.
</Note>

[Learn more about data backup.](/resources/self_hosting/data_backup/dumps)