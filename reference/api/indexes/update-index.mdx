---
title: "Update an index"
description: "PATCH /indexes/{indexUid} — Meilisearch indexes API reference."
openapi: patch /indexes/{indexUid}
---

Update an index's primary key. You can freely update the primary key of an index as long as it contains no documents.

<Warning>
To update the primary key of an index that already contains documents, you must first delete all documents in that index.
</Warning>

[Learn more about indexes.](/getting_started/glossary/indexes)