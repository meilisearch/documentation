---
title: "Create an index"
description: "POST /indexes — Meilisearch indexes API reference."
openapi: post /indexes
---

Create an index.

<Note>
An index is automatically created when adding documents using the [add documents endpoint](/reference/api/documents/add-or-replace-documents), so you only need to use this endpoint if you want to set the primary key before adding documents.
</Note>

[Learn more about indexes.](/getting_started/glossary/indexes) | [Learn more about primary keys.](/getting_started/glossary/primary_key)