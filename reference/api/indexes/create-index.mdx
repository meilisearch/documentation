---
openapi: post /indexes
---

Create an index.

<Note>
An index is automatically created when adding documents using the [add documents endpoint](/reference/api_v2/documents/add-or-replace-documents), so you only need to use this endpoint if you want to set the primary key before adding documents.
</Note>

[Learn more about indexes.](/resources/glossary/indexes) | [Learn more about primary keys.](/resources/glossary/primary_key)