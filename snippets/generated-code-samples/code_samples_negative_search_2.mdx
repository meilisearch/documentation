<CodeGroup>

```javascript JS
client.index('movies').search('-"escape"')
```

```python Python
client.index('movies').search('-"escape room"')
```

```php PHP
$client->index('movies')->search('-"escape"');
```

```rust Rust
let results = index.search()
  .with_query("-\"escape room\"")
  .execute()
  .await
  .unwrap();
```
</CodeGroup>