<CodeGroup>

```javascript JS
client.index('INDEX_NAME').search('kitchen utensils', {
  hybrid: {
    semanticRatio: 0.9,
    embedder: 'EMBEDDER_NAME'
  }
})
```

```python Python
client.index('books').search('kitchen utensils', {
  "hybrid": {
    "semanticRatio": 0.9,
    "embedder": 'EMBEDDER_NAME'
  }
})
```

```php PHP
$client->index('INDEX_NAME')->search('kitchen utensils', [
  'hybrid' => [
    'semanticRatio' => 0.9,
    'embedder' => 'EMBEDDER_NAME'
  ]
]);
```

```rust Rust
let results = index
  .search()
  .with_query("kitchen utensils")
  .with_hybrid("EMBEDDER_NAME", 0.9)
  .execute()
  .await
  .unwrap();
```
</CodeGroup>