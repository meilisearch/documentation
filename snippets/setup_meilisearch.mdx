export const SetupMeilisearch = () => (
  <Note>
    **Set up your Meilisearch connection:**
    ```bash
    export MEILISEARCH_URL="https://your-instance.meilisearch.io"
    export MEILISEARCH_API_KEY="your_api_key"
    ```
    Replace with your [Meilisearch Cloud](https://cloud.meilisearch.com) URL or self-hosted instance address.
  </Note>
);
