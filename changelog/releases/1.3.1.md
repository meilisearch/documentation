# v1.3.1 🦁

Released: 2023-08-10

# Fixes

* Fix geo bounding box with strings (#3986) @irevoire. ⚠️ You need to re-index documents containing `lat` and `lng` to make it work on already indexed documents.
* Fix panic `MDB_BAD_VALSIZE` when importing a dump in v1.3.0 (#3981) @ManyTheFish 
* Fix `meilisearch_nb_tasks` when using Prometheus and the Meilisearch's `/metrics` route (#3976) @irevoire 